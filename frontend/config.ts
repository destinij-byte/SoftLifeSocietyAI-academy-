/**
 * Placeholder. Delete this file and import from the real SLS app config
 * module instead — Academy just needs a base API URL, it doesn't own one.
 */
export const API_BASE_URL = process.env.EXPO_PUBLIC_API_BASE_URL ?? "https://api.softlifesociety.ai";
